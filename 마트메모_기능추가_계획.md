# 마트메모 — 기능 추가 계획

> 작성일 2026-09-02. 기준: 작업 트리 versionName 2.0(마트메모), Room v9, 커밋 미실행 상태.
> 이 문서는 "무엇을, 왜, 어떤 순서로" 넣을지의 단일 출처다. 각 항목은 **목적 → 범위 → 구현 메모 → 완료 기준** 순서.
> 상세 설계는 항목별로 별도 문서를 만들지 말고 이 파일에 덧붙인다.

> **진행 기록**
> - 2026-09-02: P0 완료 — `v1.0-beta` 태그, pivot 커밋(22390e5), origin 푸시. Nano 다운로드 재시도 보강(f4a3afe).
> - 2026-09-02: P1-a 매장 기록, P1-b 백업·내보내기 구현 + 유닛 테스트 17건 통과. **실기기 QA 전** — 아래 각 항목 "완료 기준"을 폰에서 확인하면 비공개 테스트 시작.

---

## 0. 지금 앱의 상태와 문제 정의

**한 줄**: 가격표를 찍으면 온디바이스로 상품명·가격을 뽑아 기록하고, 같은 상품을 다시 찍으면 지난번과의 차액을 보여주는 앱. 권한 0개, 서버 없음.

**온디바이스 AI 설치 방식(확인 2026-09-02)**: 사용자 조작 없음. 앱 시작 시 `prewarm()`이 AICore 상태를 보고 `DOWNLOADABLE`이면 Gemini Nano를 백그라운드로 받는다(시스템이 Wi-Fi·충전 등 조건 관리). 촬영 시점에도 `DOWNLOADABLE`이면 다시 건다. 미지원 기기·다운로드 전에는 규칙 인식만으로 동작하고, 설정 화면 "온디바이스 AI" 카드에서 상태를 볼 수 있다. 지원 기기는 AICore 탑재 기종(Pixel 9 이후, Galaxy S25 등)에 한정된다.

- 안내 UI(2026-09-02, 사용자 요청): 첫 실행 1회 대화상자(`OnDeviceAiIntroDialog`) — 이 폰이 지원 기기인지, 미지원이면 어떻게 동작하는지(글자 인식+규칙, 후보 칩, 가격 숫자는 항상 OCR), 지원 기종 목록, 지원 기기면 "AI 모델 지금 받기" 버튼. 설정 "온디바이스 AI" 카드(`OnDeviceAiCard`)에 같은 내용 + 진행률 + 받기/다시 받기 버튼. 문구 단일 출처는 `ui/components/OnDeviceAi.kt`.
- 지원 기종(Prompt API 문서 2026-09 확인): 갤럭시 Z Fold7·Z TriFold·S26 시리즈·Z Fold8/Flip8, 픽셀 9·10·11 시리즈. **갤럭시 S25·Z Fold6 이하 미지원**(개발 기기 폴드6 포함 → 폴드6에선 항상 "기본 인식" 경로만 검증됨).
- 다운로드 버튼 한계: 앱은 AICore에 "요청"만 가능. Wi-Fi·충전 조건은 시스템이 판단하므로 눌러도 바로 안 받을 수 있고, 그 경우 진행률 0에 머문다(문구로 안내).

**핵심 문제**: 가치("지난번보다 얼마 차이")가 **두 번째 촬영**에서야 생긴다. 첫 촬영은 수고만 있고 보상이 없다.
→ 아래 추가 기능은 전부 이 문제를 푸는 방향으로 고른다. **"첫 촬영에 보상", "기록 0건이어도 볼 것"** 두 기준.

**한 줄 목표 문장(스토어·자소서·아버지 설명용)**:
> 가격표 찍으면, 지금 사도 되는지 알려줘요.

---

## 1. 우선순위 요약

| 순서 | 항목 | 왜 이 순서인가 | 크기 |
|---|---|---|---|
| P0 | git 정리 (1.0 태그 + 2.0 커밋) | 16k줄 삭제가 커밋 없이 떠 있음. 사고 나면 복구 불가 | 10분 |
| P1-a | 매장 기록 | 매장 없이 가격 비교하면 틀림. 테스트 전 필수 | 반나절 |
| P1-b | 백업·내보내기 | 온디바이스 전용 → 폰 바꾸면 전부 소실. 부모 세대 신뢰 문제 | 반나절 |
| — | **비공개 테스트 시작 (12명+, 14일)** | P1 끝나면 바로 | — |
| P2-a | 참가격 공공데이터 연동 | 첫 촬영 보상, 빈 앱 해소, OCR 매칭 정확도, 매장 목록 자동화 | 4~5일 |
| P2-b | 가격 추이 + "지금 사도 되나" 배지 | 앱 정체성을 기록 → 판단으로 격상 | 3~4일 |
| P3 | 단위가 비교 / 행사 종료 알림 / 바코드 스캔 / 공유 카드 / 장보기 리스트 | 출시 후 반응 보고 | 각 1~3일 |

P2는 테스트 14일 동안 만들어 중간 업데이트로 배포한다 → 프로덕션 신청 시 "테스터 피드백으로 개선한 내용"이 자연스럽게 생긴다.

---

## P0. git 정리 (오늘)

**목적**: 1.0(캐릭터·일정 앱) 히스토리를 태그로 고정하고, 2.0(마트메모) 대량 삭제를 커밋으로 확정.

**할 일**
```
git tag -a v1.0-beta c9357d4 -m "Petling 1.0 beta — 캐릭터·일정·보관함 앱 (마트메모 피벗 전)"
git add -A
git commit -m "pivot: 마트메모 2.0 — 가격 추적 전용으로 정리 (캐릭터·일정·보관함 제거)"
git push origin main --tags
```
- 브랜치 `feat/ai-pipeline`, `feat/character-overlay`, `feat/character-redesign`, `feat/multimodal-classify`는 전부 main에 머지됐거나 태그로 보존되므로 삭제해도 된다(선택).

**완료 기준**: `git status` 깨끗. `git show v1.0-beta` 로 캐릭터 코드 조회 가능.

---

## P1-a. 매장 기록

**목적**: 같은 상품이 이마트/홈플러스에서 다른 가격인데 지금은 매장 구분 없이 섞인다. 재방문 비교와 P2 시장 비교 모두 매장이 축이다.

**범위**
- 확인 화면(`PriceScreen.kt` 가격표 확인 섹션)에 **매장 칩 행** 추가: 최근 사용 매장 3개 + "직접 입력".
- 저장 시 `PriceEntryEntity.storeName`에 기록(필드는 이미 있음, 스키마 변경 없음).
- 목록(`TrackedProduct`)의 차액 계산은 **같은 매장의 직전 기록** 우선, 없으면 전체 직전 기록으로 폴백하고 UI에 "다른 매장 기준" 표시.
- 상품 상세(`PriceProductScreen.kt`) 이력 행에 매장명 표시.
- **위치 권한은 쓰지 않는다**(권한 0개 원칙). P2에서 참가격 판매점 목록으로 자동완성만 붙인다.

**구현 메모**
- 최근 매장 = `price_entries`에서 `storeName` distinct, `createdAt` desc limit 3 → `PriceDao`에 쿼리 1개 추가.
- `PriceViewModel.AnalysisUi.Ready`에 `storeName: String?` 추가, `save()`에서 전달.
- `PriceRepository.save()` 시그니처에 `storeName` 추가. `latestEntry(productId)` 옆에 `latestEntry(productId, storeName)` 추가.

**완료 기준**: 같은 상품을 매장 A, B로 나눠 찍었을 때 목록 차액이 매장별로 계산되고, 상세에서 매장명이 보인다.

**상태(2026-09-02)**: 구현 완료, 실기기 확인 대기. 구현 결정 사항:
- 오늘 이미 기록한 매장이 있으면 그 매장을 기본 선택(같은 장보기 중일 확률이 높음). 첫 사용은 입력창부터 노출.
- 매장명 비교는 공백·대소문자 무시(`normalizeStoreName`), 저장은 입력 그대로.
- 비교 규칙은 `domain/price/StoreCompare.kt`(순수 Kotlin, `StoreCompareTest` 6건)로 분리해 목록·확인 화면이 같은 규칙을 쓴다.
- 매장 미기록 과거 데이터(1.0 베타)는 매장을 고르면 "다른 매장" 취급 → "매장 정보가 없는 기록과 비교해요" 안내.

---

## P1-b. 백업 · 내보내기

**목적**: `allowBackup=false` + 온디바이스 → 기기 교체 시 데이터 전부 소실. 부모 세대는 폰을 바꾸면 그 순간 앱을 버린다.

**범위**
- 설정 화면(`SettingsScreen.kt`)에 **"내보내기"**, **"가져오기"** 두 항목.
- 형식: JSON 한 파일(`products[]` + `entries[]`). 사진은 제외(용량·복잡도). CSV는 P3에서 필요 시.
- 저장 위치는 시스템 파일 선택기(`ActivityResultContracts.CreateDocument` / `OpenDocument`) → 권한 불필요.
- 가져오기는 **병합**(바코드 → 정규화 이름 순으로 기존 상품에 붙이고, 같은 `dateEpochDay`+`priceWon` 기록은 중복 스킵).

**구현 메모**
- `data/backup/BackupCodec.kt`(순수 Kotlin, kotlinx-serialization) + `BackupRepository`(DAO 읽기/쓰기). 유닛 테스트: 내보내기→가져오기 왕복 동일성.
- `android:allowBackup`은 계속 false 유지(Google 자동 백업은 기기 간 예측 불가). 수동 파일이 진실원.

**완료 기준**: 내보내기 파일을 앱 삭제 후 재설치해 가져오면 목록·이력이 동일.

**상태(2026-09-02)**: 구현 완료, 실기기 확인 대기(특히 삼성 내 파일·Google 드라이브 경유 가져오기). 구현 결정 사항:
- 파일: `{"format":"martmemo-backup","version":1, products[], entries[]}`. 필드는 추가만 하고 삭제·의미 변경 금지, 읽을 때 모르는 키 무시(상위 버전 파일도 최대한 읽음, 더 높은 version은 거부).
- 사진·네이버 스냅샷 필드 제외. 상품이 없는 고아 기록 제외.
- 가져오기는 한 트랜잭션(중간 실패 시 아무것도 남지 않음). 결과 스낵바: 새 상품/새 기록/중복 건너뜀 수.
- `BackupCodecTest` 5건: 왕복 동일성, 재가져오기 전부 중복, 이름 매칭 병합, 형식 거부, 미지 키 무시.

---

## ▶ 여기서 비공개 테스트 시작

- 테스터 15명 이상 확보(12명 요건 + 여유). 아버지 지인 6 + 학과 6 + 예비.
- 부탁 문구: "설치하고 이틀에 한 번 열어서 가격표 하나만 찍어주세요. 14일." (2026년부터 실사용 여부를 봄)
- 스토어 등록정보를 마트메모 기준으로 갱신(스크린샷 새로 촬영, 구 펫 앱 자산 폐기). 개인정보처리방침 Notion 교체.
- 테스트 중 피드백은 이 문서 맨 아래 "테스터 피드백" 섹션에 날짜와 함께 적는다.

---

## P2-a. 참가격(한국소비자원) 공공데이터 연동

**목적**: 첫 촬영에서 "이 매장 6,980원 / 근처 대형마트 중앙값 6,480원"을 보여준다. 기록 0건이어도 앱을 열 이유를 만든다. 540개 상품명 사전으로 OCR 매칭을 교정한다.

**데이터**: 공공데이터포털 "한국소비자원_생필품 가격 정보" 오픈 API(2건). 262개 판매점(대형마트 119·SSM 91·백화점 28·전통시장 21·편의점 3), 158품목 540상품, 격주 조사. 판매업소명·판매가격·조사일·세일여부 포함.

**먼저 확인할 것(가입 후 30분)**
- [x] 가격 조회 오퍼레이션 확인(`getProductPriceInfoSvc`). [ ] 상품정보·판매점정보 오퍼레이션 이름·파라미터(상세 페이지 캡처 필요)
- [x] 과거 조사일 조회: **2026-06-12 이후만 가능** → P2-b는 누적 또는 파일 백필
- [ ] 일일 호출 한도(개발 계정 기본 한도 확인)
- [x] 응답에 판매점 ID(`entpId`) 있음. 지역 필드는 가격 응답엔 없음 → 판매점정보 오퍼레이션에서 확인

**API 실측(2026-09-02, GW 개발계정 승인 후 직접 호출)**
- 구형 API(3043385)는 신청 폼 404 → 폐기. **"한국소비자원_생필품 가격 정보_GW"만 사용.** 엔드포인트 `https://apis.data.go.kr/B551919/ProductPriceInfoService`, XML. 인증키는 `local.properties`의 `kca.serviceKey`(커밋 금지, 채팅 노출된 키라 재발급 권장).
- 가격 조회: `getProductPriceInfoSvc?goodInspectDay=YYYYMMDD&entpId=N` 또는 `&goodId=N`(둘 중 하나 필수). **판매점 1개 호출 = 그 조사일의 전 상품 약 340행**, 페이징 없음(numOfRows·pageNo 무시), 응답 약 90KB. 상품 1개 호출 = 전 판매점 약 440행. 응답 필드: `goodInspectDay, entpId, goodId, goodPrice, inputDttm` — **세일 여부·판매점명·상품명 없음**(id만).
- 조사 주기: 격주 금요일(06-12, 06-26, 07-10, 07-24, 08-07, 08-21) + 08-28도 있음(주간 전환 가능성). 조사 후 약 6일 뒤 입력(`inputDttm` 07-30).
- **과거 조회는 2026-06-12부터만 된다.** 그 이전(2025-12~2026-05 격주 금요일, 2026-03~06-11 매일, 2019~2025 7~8월 금요일) 전부 빈 응답. → P2-b "6개월 분포"는 앱이 스냅샷을 누적하거나 파일데이터(생필품 가격동향 XLS)로 백필해야 함.
- 전체 동기화 비용: 판매점 기준 약 440회/조사일(개발계정 일일 한도 확인 필요, 통상 1,000). 상품 기준 약 340회로 더 적음 → **상품(goodId) 기준 루프**가 유리. 판매점·상품 id→이름 매핑 오퍼레이션은 아직 미확인(상세 페이지 오퍼레이션 목록 필요).

**설계 원칙 — 촬영 때 API를 부르지 않는다**
- 앱이 **격주로 전체 데이터를 받아 로컬에 저장**한다(540상품 × 262매장 ≈ 수 MB). 사용자 사진·상품명은 밖으로 나가지 않는다 → "온디바이스, 권한 0개" 유지, 마트 지하 오프라인에서도 동작.
- 최초 실행 시 최근 1회분만 받고, 과거 12개월 백필은 백그라운드에서.
- 개인정보처리방침에 "공공데이터 다운로드(개인정보 전송 없음)" 한 줄 추가.

**범위**
1. **로컬 시장 데이터 저장** — Room v10
   - `market_stores(entpId, name, type, region)`
   - `market_products(goodId, name, normalizedName, category, spec)` ← OCR 교정 사전
   - `market_prices(goodId, entpId, inspectDay, priceWon, isSale)` (goodId+entpId+inspectDay 유니크)
   - `MIGRATION_9_10`: 테이블 3개 추가만. 기존 데이터 무변경.
2. **동기화** — `data/market/MarketSyncWorker`(WorkManager 격주, 충전 중·Wi-Fi 제약) + 설정 화면에 "마지막 갱신일 / 지금 갱신".
3. **상품 매칭** — `domain/market/MarketMatcher`(순수 Kotlin)
   - 입력: `PriceTagInfo`(이름·용량·바코드). 출력: `goodId?` + 신뢰도.
   - 규칙: 정규화 이름 토큰 겹침 ≥ 2 + 용량 일치 → 확정. 그 외 후보 3개까지 → 확인 화면에서 사용자가 고른다("이 상품 맞나요?").
   - 사용자가 고른 매핑은 `price_products.marketGoodId`에 저장해 다음부턴 자동. (v10에 컬럼 추가)
   - 유닛 테스트: 골든 가격표 4장(존쿡·파채·영리프·떡볶이) + 계란·우유·라면 케이스 추가.
4. **확인 화면 카드** — "🏷 시장 가격" 카드: 이 매장 최근 조사가 / 전국 같은 업태 중앙값 / 차이 %. 매장이 참가격 목록에 없으면 중앙값만.
5. **매장 칩 자동완성** — P1-a의 "직접 입력"에 `market_stores` 이름 검색 붙임.

**완료 기준**: 참가격에 있는 상품(계란 특란 30구, 서울우유 1L, 신라면 5입) 가격표를 처음 찍었을 때 저장 전에 시장 비교가 뜬다. 참가격에 없는 상품은 기존 흐름 그대로.

**한계(문서·UI에 명시)**: 540상품 한정(PB·정육·채소 낱개 제외, 체감 매칭률 절반 이하). 격주 조사라 이번 주 행사가 미반영 → "정가 기준". 동네 소형 마트 없음.

---

## P2-b. 가격 추이 + "지금 사도 되나" 배지

**목적**: 앱을 "기록"에서 "판단"으로 격상. 사용자가 원하는 건 그래프가 아니라 **"싼 편 / 보통 / 비싼 편"** 한 단어다.

**범위**
1. **상품 상세 스파크라인** — Compose Canvas 직접 그리기(라이브러리 없음). 선 두 개:
   - 시장선: `market_prices`에서 사용자가 고른 매장(없으면 같은 업태 중앙값), 최근 12개월, 격주 점.
   - 내 기록: 점으로 겹쳐 찍는다. → "나는 시장보다 위/아래에서 사고 있었다"가 보인다.
2. **판단 배지** — `domain/market/BuySignal`(순수 Kotlin, 규칙만)
   - 최근 6개월 시장가 분포에서 현재가(오늘 찍은 가격, 없으면 최신 시장가)의 백분위.
   - 하위 30% → "싼 편", 상위 30% → "비싼 편", 나머지 "보통". 데이터 6점 미만이면 배지 없음.
   - 확인 화면 시장 카드와 상품 상세 상단에 표시.
   - 유닛 테스트: 분위 경계, 데이터 부족, 세일 플래그 처리.
3. **홈 "자주 사는 상품"** — 기록 3회 이상 또는 별표(★) 상품을 홈 상단 섹션에 배지와 함께. `price_products.isStarred`(v10 컬럼).
4. 그래프 아래 "한국소비자원 참가격, 격주 조사 기준" 출처 한 줄.

**완료 기준**: 계란 상세에서 12개월 시장선 + 내 기록 점 + 배지가 보이고, 참가격에 없는 상품은 내 기록 점만 보인다.

---

## P3. 출시 후 후보 (반응 보고 순서 결정)

| 항목 | 한 줄 | 메모 |
|---|---|---|
| 단위가 비교 | 이미 뽑는 `unitPriceWon`으로 같은 카테고리끼리 g당 가격 나란히 | 데이터 있음, 화면만 |
| 행사 종료 알림 | `saleEndEpochDay` 전날 알림 | 1.0 `notifications/ScheduleAlarmScheduler`·`BootCompletedReceiver`를 `git show v1.0-beta:` 로 복구. 알림 권한 1개 추가됨 |
| 바코드 스캔 | ML Kit barcode-scanning(온디바이스) | `barcode` 필드 있음. OCR보다 매칭 정확 |
| 공유 카드 | "계란 30구 지난달 6,980 → 오늘 8,480 ▲" 이미지 → 카톡 | 부모 세대 단톡 확산 = 테스터 모집 |
| 장보기 리스트 | 집에서 체크 → 마트에서 찍으면 완료 | 앱 여는 시점을 "마트 가기 전"으로 앞당김 |
| CSV 내보내기 | P1-b JSON 외 엑셀용 | 요청 있으면 |

---

## 하지 않을 것

- 캐릭터·게임화 복구 — 단순함이 이 앱의 강점. 필요하면 v1.0-beta 태그에 있다.
- 온라인 최저가 수치 비교 — 네이버 쇼핑 API 2026-07 종료, 쿠팡은 승인 불가, 스크래핑은 약관 위반. 딥링크로 충분.
- 계정·서버·클라우드 동기화 — 권한 0개·온디바이스 원칙과 충돌. 백업은 파일로.
- 위치 권한 — 매장은 수동 선택 + 자동완성으로.
- iOS — 반응 확인 전에는 없음.

---

## 테스터 피드백 (테스트 중 기록)

| 날짜 | 누가(연령대) | 내용 | 대응 |
|---|---|---|---|
| | | | |

---

## 열린 질문

- ~~참가격 API 과거 조회 가능 여부~~ → 확인됨: 2026-06-12부터만. 선택지: ① 앱이 격주 스냅샷 누적(출시 후 6개월 뒤 배지) ② 파일데이터 "생필품 가격동향"(XLS)로 과거 12개월을 앱 자산에 내장해 첫 배포부터 배지 ③ 배지 기준을 "최근 3개월"로 낮춤. **②+③ 조합 검토.**
- 참가격 판매점에 사용자 매장이 없을 때 시장 카드의 기본 비교 대상(같은 업태 전국 중앙값 vs 같은 시도 중앙값). 지역 필드 있으면 후자.
- 계란·우유처럼 규격 여러 개인 상품의 매칭 UI — 후보 3개 칩으로 시작, 반응 보고 개선.
