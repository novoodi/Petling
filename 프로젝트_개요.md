# Petling — 프로젝트 개요 (as-built)

> 이 문서는 **실제 구현된 코드 기준**의 개요입니다. 루트의 기획 문서
> (`기술스택_아키텍처.md`, `MVP_기능명세서.md` 등)와 달리 현재 소스 트리에 반영된
> 사실만 담습니다. 기획과 구현이 갈린 부분은 "기획 vs 실제"로 표시했습니다.
>
> 기준 커밋: `feat/character-redesign` 브랜치 · Room DB v7 · versionName 1.0

---

## 1. 무슨 앱인가

**한 줄 정의**: 화면 캡처·말 한마디·붙여넣기 텍스트에서 **온디바이스 AI**로 일정을 추출해 등록하고,
지킨 일정을 에너지원으로 **내 캐릭터가 함께 자라는** 다마고치형 일정 앱 (Android, 10~20대 학생 타깃).

**차별화 2축**
1. **온디바이스 우선** — 캡처 이미지·음성·텍스트는 원칙적으로 기기 밖으로 나가지 않는다. 네트워크 전송은 익명 크래시 리포트 정도로 제한.
2. **나만의 캐릭터 육성** — 일정 관리 성적표를 귀엽게 대신 보여주는 존재. 소유감(내 것)·성취 가시성·공유욕을 설계 축으로 삼는다.

---

## 2. 주요 기능 (기획 vs 실제 구현)

NavHost(`ui/navigation/PetlingNavHost.kt`)에 연결된 실제 화면 기준.

| 기능 | 구현 패키지 | 상태 |
|---|---|---|
| 온보딩(랜덤 부화 + 성격 퀴즈) | `ui/onboarding/` | ✅ 구현 |
| 홈(로밍 캐릭터 + 오늘 일정 + 말풍선) | `ui/home/` | ✅ 구현 |
| 캡처 → 일정 확인·수정 | `ui/capture/` | ✅ 구현 |
| 음성 입력(STT) | `ui/voice/` | ✅ 구현 |
| 캘린더(일/주/월) | `ui/calendar/` | ✅ 구현 |
| 일정 상세/수정 | `ui/schedule/` | ✅ 구현 |
| 캐릭터 상세 + 성장 다이어리 | `ui/characterdetail/` | ✅ 구현 |
| 설정 | `ui/settings/` | ✅ 구현 |
| **텍스트 붙여넣기 → 일정 추출** | `ui/paste/` | ✅ 구현 (기획상 P1이 이미 반영) |
| **캡처 보관함/정리함(7종 분류)** | `ui/library/` | ✅ 구현 (기획 확장) |
| **맞춤 분류 카테고리 관리** | `ui/category/` | ✅ 구현 (기획 확장) |

> **기획 확장 주의**: `library`(정리함), `paste`, `category`, 캡처 7종 분류(일정·공부·대화·링크·장소·쇼핑·추억)는
> 원본 기획 3개 MD에는 없던 실제 확장 기능입니다. 스크린샷을 "일정"뿐 아니라 카테고리로 분류·보관합니다.

**게임화 요소**: 누적 완료(캡처) 건수 기반 성장, XP·연속 이행 스트릭, 호감도(0~100) 게이지, 간식 주기, 성격별 문구.

---

## 3. 기술 스택 (정확한 버전)

출처: `gradle/libs.versions.toml`, `app/build.gradle.kts`

### 앱 식별 / 빌드 타깃
| 항목 | 값 |
|---|---|
| applicationId (Play 식별자) | `com.petling.app` |
| namespace (코드 패키지) | `com.example.petling` |
| versionCode / versionName | 1 / `1.0` |
| compileSdk / targetSdk / minSdk | 36 / 36 / 31 |
| Java / Kotlin JVM target | 11 |

### 언어 · 빌드 도구
- **Kotlin** 2.2.0 · **AGP** 8.11.2 · **KSP** 2.2.0-2.0.2
- Gradle 플러그인: kotlin-android, kotlin-compose, ksp, kotlin-serialization, google-services, firebase-crashlytics

### UI (Jetpack Compose)
- **Compose BOM** 2024.09.00 (Material3, ui, ui-graphics, material-icons-extended)
- **Navigation Compose** 2.8.9 (type-safe routes)
- activity-compose 1.13.0 · lifecycle(runtime/viewmodel/runtime-compose) 2.9.4 · core-ktx 1.17.0

### 데이터 · 로컬
- **Room** 2.7.2 (runtime/ktx/compiler[KSP]) — `exportSchema=true`
- **DataStore Preferences** 1.1.4
- **kotlinx-serialization-json** 1.7.3
- kotlinx-coroutines(-test / -play-services) 1.9.0

### 온디바이스 AI
- **ML Kit Text Recognition Korean** (OCR) `com.google.mlkit:text-recognition-korean` 16.0.1
- **ML Kit GenAI Prompt** (Gemini Nano) `com.google.mlkit:genai-prompt` 1.0.0-beta2
- play-services-tasks 18.2.0
- STT: Android 기본 **SpeechRecognizer** (온디바이스)

### 모니터링
- **Firebase BOM** 33.7.0 — Crashlytics · Analytics · google-services 플러그인 4.4.2 · crashlytics 플러그인 3.0.2

### 테스트
- JUnit4 4.13.2 · androidx.test.ext:junit 1.3.0 · espresso 3.7.0 · compose ui-test-junit4

> **3D/애니메이션 엔진 없음**: 캐릭터는 Lottie·Rive·Sceneview/Filament 같은 엔진이 아니라
> **Jetpack Compose Canvas로 직접 그리는 순수 2D 파라메트릭 벡터**입니다.
> (기획서는 Lottie/Rive를 제안했으나 자체 Canvas 드로잉으로 구현됨.)

---

## 4. 아키텍처

### 레이어 구조
```
com.example.petling
├── domain/          순수 Kotlin 비즈니스 로직 (Android 비의존)
│   ├── model/       CharacterSpec, CharacterState, Species, Enums, Schedule …
│   ├── engine/      XpEngine, GrowthStateMachine, BranchResolver, MoodCalculator, StreakCalculator
│   ├── parsing/     RuleBased/Korean/Composite ScheduleParser (규칙 기반 폴백)
│   ├── capture/     RuleBasedCaptureClassifier, CaptureBranchResolver, UrlExtractor …
│   └── personality/ PersonalityQuiz, PhraseSelector, PhraseBank
├── data/
│   ├── local/       Room(entity·dao·PetlingDatabase·Converters·Migrations)
│   ├── repository/  Capture/Category/Character/Schedule Repository
│   ├── capture/     OcrTextExtractor, GeminiNanoCaptureClassifier
│   ├── parsing/     GeminiNanoScheduleParser
│   └── datastore/   SettingsDataStore
├── ui/              Compose 화면 + navigation, theme, character(렌더링), components
├── notifications/   AlarmManager·JobScheduler 기반 알림/감지
└── di/AppContainer  수동 DI 컨테이너
```

### DI — 수동 DI (Hilt 미사용)
`di/AppContainer.kt`가 앱 전역 의존성을 생성자 주입으로 조립하고, `PetlingApplication`이 보유,
UI는 `ui/LocalContainer.kt`(CompositionLocal)로 접근. 각 화면은 전용 ViewModel을 둔다.

### Room (`data/local/PetlingDatabase.kt`)
- DB `petling.db`, **version 7**, 엔티티 6종 / DAO 6종
  - 엔티티: Schedule · CharacterState · XpLog · GrowthSnapshot · Capture · Category
- **마이그레이션 정책**: 파괴적 재생성 금지, 명시적 마이그레이션만. 현재 `MIGRATION_6_7`(호감도 컬럼 4개) 적용.

### 온디바이스 AI 파이프라인 — "Nano → 규칙 기반 폴백"
```
스크린샷/이미지 ─▶ OCR(ML Kit) ─┐
음성 ─▶ STT(SpeechRecognizer) ─┤─▶ 원시 텍스트 ─▶ 일정 파싱
붙여넣기 텍스트 ───────────────┘        1차: Gemini Nano(genai-prompt)
                                        2차: 규칙 기반 파서(정규식+한국어 날짜)
                                              ▼
캡처 분류: Gemini Nano(활성 카테고리 내 응답) → 실패 시 규칙 기반
                                              ▼
                                   사용자 확인/수정 UI ─▶ Room 저장
```
- Nano 미지원/DOWNLOADABLE/실패 시 `Composite*`가 규칙 기반으로 자동 폴백 → **저사양·미지원 기기에서도 핵심 기능 동작**.
- 지원 기기는 앱 시작 시 `prewarm()`으로 모델 사전 다운로드(비차단).
- Nano는 오늘 날짜를 프롬프트에 주입해 "내일·다음 주 화요일" 같은 상대 날짜를 절대 날짜로 계산.

### 알림 / 백그라운드 (`notifications/`)
- **정확 알람**: `ScheduleAlarmScheduler` — **AlarmManager** `setExactAndAllowWhileIdle`(권한 없으면 `setWindow` 폴백). 일정당 최대 2개(전날/당일). **WorkManager 미사용.**
- **스크린샷 감지**: `ScreenshotDetectJobService` — **JobScheduler**가 MediaStore 콘텐츠 URI 변화를 트리거(상시 서비스 없이 이벤트 기반).
- **부팅 복원**: `BootCompletedReceiver`(BOOT_COMPLETED/TIME_SET/TIMEZONE_CHANGED → 알람 재등록).
- 공유 인텐트로 `image/*` · `text/plain` 수신.

---

## 5. 캐릭터 시스템

**렌더링**: `ui/character/CanvasCharacterRenderer.kt`가 Compose Canvas에 `drawCreature`로 직접 그림.
종별 드로잉 9파일(`species/`) + 공통 파츠(해부학·얼굴·액세서리·이펙트·홉·팔레트).

| 축 | 정의 위치 | 요약 |
|---|---|---|
| 종(9종) | `domain/model/Species.kt` | 도토리(마스코트, 비선택)·여우·고양이·토끼·병아리·강아지·햄스터·펭귄·판다. 몸은 종 고유 코트 컬러 고정, hue는 소품에만 |
| 성장 4단계 | `domain/model/Enums.kt` | JUVENILE→GROWTH1→GROWTH2→MATURE. **전이 기준은 XP가 아니라 누적 완료(캡처) 건수**. 크기뿐 아니라 해부학 비율이 변함 |
| 성격 4종 | `Enums.kt` (Personality) | 성실·자유분방·걱정·몽상. 알림 문구 + **렌더 모션 리듬**에 반영 |
| 체급 | `CreatureAnatomy.kt` `bulkFor()` | 성숙기 병아리 0.60 ~ 판다 1.25, 바닥선 기준 스케일 |
| 체형 아키타입 | `BodyPlan` enum | 오뚝이·웅크린 공·앉은 4족·묵직한 반원·엉덩이 뭉치 5종 |
| 개체 시드 변이 | `IndividualTraits.kt` | `hatchedAt`을 seed로 재사용, splitmix64로 크기·볼·꼬리 미세차 결정. 코트·실루엣 불변 |
| 성격 모션 | `PersonalityMotionProfile.kt` | 성격별 숨쉬기·깜빡임·홉 주기·살랑 진폭·눈꺼풀. 기본=기존 상수(회귀 없음) |

`CharacterSpec`은 `@Serializable`이라 성장 다이어리 스냅샷(`growth_snapshots`)에 저장·재렌더된다.

> **최근 리팩토링(feat/character-redesign)**: 사실적 4족 옆모습 보행을 **정면 홉 보행**으로 교체,
> 종별 체급 도입, 체형 아키타입 5종 분리, 시드 개체 변이, 성격 모션 프로파일을 추가.

---

## 6. 테스트 / 품질

- **유닛 테스트**(`app/src/test/`, JUnit4 + coroutines-test) — 전부 `domain` 순수 로직 + 렌더 파라미터:
  - 게임 로직: XpEngine·StreakCalculator·MoodCalculator·GrowthStateMachine·BranchResolver·AffectionRules
  - 파싱/분류: KoreanScheduleParser·CaptureClassifier·UrlExtractor·ReminderTime·ScreenshotDetector
  - 캐릭터: CharacterSpecSerialization·PersonalityQuiz·PhraseSelector + **BodyPlan·BulkFor·HopPose·IndividualTraits·PersonalityMotionProfile**
- 계측 테스트는 기본 템플릿 1개(`ExampleInstrumentedTest`).
- **Crashlytics**: 플러그인·의존성·`google-services.json` 배선 완료. 자동 초기화 상태이며 수동 `recordException` 승격은 TODO.

---

## 7. 빌드 / 배포

- **릴리스 서명**: 루트 `keystore.properties`(git 제외)에서 로드 → 있으면 릴리스 서명, 없으면 미서명 빌드로 진행(빌드 안 깨짐). 참조 형식은 `keystore.properties.template`.
- **릴리스 최적화**: R8 `isMinifyEnabled=true` + `isShrinkResources=true` + proguard 규칙.
- **출시 준비**: `docs/LAUNCH_CHECKLIST_ko.md`(Play 계정·아이콘·키스토어·Firebase·처리방침·AAB·비공개 테스트 7단계),
  개인정보처리방침 `docs/PRIVACY_POLICY_ko.md`(온디바이스 원칙 + Crashlytics 익명 진단만 외부 전송).
- **수익 모델**(기획): 코스메틱 IAP + 리워드 광고 하이브리드. 실제 결제/IAP 코드·billing 의존성은 아직 없음(기획 단계).

---

## 부록 — 문서 정합성 메모(stale 주의)

작성 시점 기준, 일부 기존 문서가 실제 코드와 어긋납니다.

- `docs/CRASHLYTICS_SETUP.md`: "플러그인 아직 미적용"이라 서술하나 **실제로는 적용 완료** + `google-services.json` 존재.
- `기술스택_아키텍처.md`: 캐릭터 렌더를 **Lottie/Rive**로 제안하나 실제는 **Compose Canvas 2D 자체 드로잉**.
- 같은 문서의 백그라운드 스택 **WorkManager**는 실제 미사용(**AlarmManager + JobScheduler**).
