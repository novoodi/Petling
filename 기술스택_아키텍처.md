# 기술 스택 & 아키텍처 명세서 (Android MVP)

## 0. 설계 원칙
1. **온디바이스 우선**: 캡처 이미지·음성 데이터는 원칙적으로 기기 밖으로 나가지 않는다.
2. **저사양 기기 대응**: 학생층은 중저가폰 비중이 높으므로, 최신 AI 기능이 없는 기기에서도 핵심 기능이 동작해야 한다(폴백 필수).
3. **배터리·성능 절약**: 상시 백그라운드 프로세스 대신 이벤트 트리거 방식으로 동작한다.

---

## 1. 전체 아키텍처 개요

```
[스크린샷 촬영/이미지 선택] ─┐
[음성 입력]                ─┼─▶ [입력 수집 레이어]
                              │
                              ▼
                     [텍스트 추출 레이어]
                     - OCR (ML Kit Text Recognition)
                     - STT (Android SpeechRecognizer, on-device)
                              │
                              ▼
                     [일정 파싱 레이어]
                     - 1차: 온디바이스 LLM (ML Kit GenAI API / Gemini Nano)
                     - 2차 폴백: 규칙 기반 파서 (정규식 + 한국어 날짜 라이브러리)
                              │
                              ▼
                     [사용자 확인/수정 UI]
                              │
                              ▼
                     [로컬 저장 레이어] (Room DB)
                     - 일정 테이블 / 캐릭터 상태 테이블 / XP 로그
                              │
                     ┌────────┼────────┐
                     ▼        ▼        ▼
              [캘린더 연동] [알림 스케줄러] [캐릭터 렌더링/애니메이션]
              (CalendarContract, 선택적)   (WorkManager+AlarmManager)  (Lottie/Rive)
```

모든 처리가 기기 내부에서 끝나며, 네트워크 통신은 앱 업데이트·크래시 리포트(익명) 등 최소한의 경우로 제한합니다.

---

## 2. 레이어별 기술 스택

| 레이어 | 기술 | 비고 |
|---|---|---|
| UI | Kotlin + Jetpack Compose | Material 3 기반, 캐릭터 화면은 커스텀 Canvas/Lottie 조합 |
| 캡처 감지 | MediaStore ContentObserver | 스크린샷 저장 디렉토리 변화 감지 → 오버레이 제안 |
| OCR | ML Kit Text Recognition (on-device) | 한글 포함 다국어 지원, 무료 |
| STT | Android SpeechRecognizer (on-device 모드) | 기기 지원 여부 확인 필요 |
| 일정 파싱(1차) | ML Kit GenAI API (Gemini Nano) | 지원 기기 한정 (아래 3장 참고) |
| 일정 파싱(폴백) | 정규식 + 한국어 날짜/시간 파서 (자체 구현 또는 오픈소스) | 전 기기 커버리지 확보용 |
| 로컬 DB | Room (SQLite) | 일정, 캐릭터 상태, XP 로그, 성장 다이어리 스냅샷 |
| 알림 | WorkManager + AlarmManager + NotificationCompat | 정확한 시간 알림은 AlarmManager, 반복 작업은 WorkManager |
| 캘린더 연동(선택) | CalendarContract Provider API | 사용자가 켠 경우에만 기본 캘린더 앱과 동기화 |
| 캐릭터 애니메이션 | Lottie 또는 Rive | 표정/이펙트 리소스 적은 용량으로 다양한 반응 구현 |
| 백업(선택) | 로컬 파일 export/import, 사용자 트리거 시 Google Drive 백업(App Data) | 자동 클라우드 상시 동기화는 MVP 제외 |
| 크래시/성능 모니터링 | Firebase Crashlytics (사용 데이터 아님, 익명 크래시 로그만) | 온디바이스 원칙과 상충되지 않도록 개인정보 미포함 범위로 한정 |

---

## 3. 온디바이스 AI 파이프라인 상세

### 3.1 Gemini Nano (ML Kit GenAI API) 지원 범위
- 최신 플래그십/고급 기기 중심으로 지원되며, 중저가폰은 미지원인 경우가 많습니다.
- 앱 최초 실행 시 기기의 Nano 지원 여부를 확인하고, 미지원 시 자동으로 폴백 파서로 전환합니다(사용자에게는 "AI 인식 모드"로 안내, 기술적 차이는 노출하지 않음).

### 3.2 폴백 파서 설계 (저사양 기기 대응)
- 날짜/시간 정규식 패턴 매칭 (예: "3월 15일", "다음 주 화요일", "오후 3시")
- 장소 키워드 사전(학원/병원/카페 등 자주 등장하는 명사) 기반 추출
- 정확도는 LLM 대비 낮으므로, 폴백 모드에서는 "확인 필요" 상태 비중을 높여 사용자 수정을 유도

### 3.3 파싱 결과 스키마 (예시)
```json
{
  "title": "학원 상담",
  "date": "2026-07-14",
  "time": "15:00",
  "location": "OO학원",
  "confidence": 0.82,
  "source": "capture" // capture | voice
}
```
confidence 값이 낮으면 확인 화면에서 해당 필드를 강조 표시해 사용자가 우선 검토하도록 유도합니다.

---

## 4. 로컬 데이터 저장 구조 (개략 스키마)

| 테이블 | 주요 필드 |
|---|---|
| schedules | id, title, date, time, location, source, is_completed, created_at |
| character_state | level, stage, current_xp, mood, personality_type, evolved_branch |
| xp_log | id, schedule_id, xp_amount, reason, timestamp |
| growth_snapshots | id, stage, image_ref, achieved_at |

민감 정보(캡처 이미지 원본)는 파싱 완료 후 사용자 설정에 따라 즉시 삭제하거나, 암호화된 앱 내부 저장소(Scoped Storage)에만 보관합니다.

---

## 5. 알림/캘린더 연동 설계
- **정시 알림**: AlarmManager의 정확한 알람(exact alarm) API 사용, Android 12+ 권한 정책 대응 필요
- **캐릭터 보이스 알림**: 성격 타입별 문구 템플릿을 로컬에 내장, 서버 문구 다운로드 없이 동작
- **기본 캘린더 동기화**: 선택 기능으로 제공, 켤 경우 CalendarContract에 이벤트 삽입 (양방향 동기화는 MVP 범위 밖)

---

## 6. 기기 호환성 대응 전략

| 시나리오 | 대응 |
|---|---|
| Gemini Nano 지원 기기 | 온디바이스 LLM 파싱 (고정확도) |
| Nano 미지원 기기 | 규칙 기반 폴백 파서 + 사용자 확인 비중 강화 |
| STT 온디바이스 미지원 기기 | 온라인 STT로 폴백하되, 이 경우 사용자에게 명시적 동의 요청(프라이버시 원칙 예외 고지) |
| 저사양 기기 애니메이션 성능 | Lottie 대신 정적 이미지 + 간단 전환 애니메이션으로 다운그레이드 |

---

## 7. 프라이버시/보안 설계
- 캡처/음성 원본 데이터는 파싱 완료 즉시 폐기 옵션 기본값 On
- 온보딩에서 "이 앱은 여러분의 화면을 서버로 보내지 않습니다"를 명시적으로 안내 (차별화 메시지이자 신뢰 확보 장치)
- 온라인 STT 폴백처럼 예외적으로 네트워크를 타는 경우, 매번 사용자에게 고지 (자동 상시 처리 금지)

---

## 8. 성능/배터리 최적화 고려사항
- 스크린샷 이벤트 트리거 방식으로 상시 리스닝 최소화
- 온디바이스 LLM 추론은 비용이 크므로, 캡처당 1회 호출로 제한하고 재시도는 사용자가 명시적으로 요청할 때만 수행
- 캐릭터 애니메이션은 화면에 보일 때만 렌더링(백그라운드 리소스 절약)

---

## 9. 향후 iOS 포팅 고려사항
- 텍스트 추출: Vision 프레임워크(OCR)
- 파싱: iOS 26 Foundation Models 프레임워크
- STT: 온디바이스 Speech 프레임워크
- 공통 로직(파싱 스키마, XP 시스템, 캐릭터 상태 머신)은 플랫폼 독립적으로 설계해 두면 포팅 비용 절감

---

## 10. 기술 리스크 및 검토 사항
- Gemini Nano API는 상대적으로 신생 기능이라 기기별 동작 편차 검증 필요 (출시 전 QA 매트릭스 구성 권장)
- 폴백 파서의 정확도가 사용자 경험 하한선을 좌우하므로, 초기 개발 리소스를 폴백 품질에도 충분히 배분해야 함
- Android 알림/알람 관련 OS 정책(배터리 최적화, Doze 모드)이 버전별로 달라 정시 알림 안정성 테스트 필요
