# [가칭] 온디바이스 AI 다마고치 일정 앱 — MVP 기능 명세서

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 플랫폼 | Android (MVP) → iOS 확장 |
| 핵심 타겟 | 10~20대 학생 |
| 핵심 가치 제안 | "화면 캡처/말 한마디로 일정 등록 + 내 캐릭터가 함께 자란다" |
| 차별화 포인트 | ① 온디바이스 처리(서버 전송 없음, 프라이버시) ② 나만의 캐릭터 육성 |

### 타겟 재정의 (학생 관점)
학생에게 "일정"은 직장인과 다릅니다. 시험 범위 공지 캡처, 학원 시간표 문자, 친구가 카톡으로 보낸 약속, 학교 앱 공지사항 캡처 등이 핵심 입력원이 됩니다. 캐릭터 육성도 "성취감" 요소가 커야 하며, 친구와 캐릭터를 비교/공유하는 소셜 요소가 학생층에서는 특히 중요합니다.

---

## 1. 핵심 사용자 시나리오 (User Flow)

1. **캡처 등록**: 학원 앱/카톡 공지 스크린샷 촬영 → 알림 배너 "일정으로 등록할까요?" 탭 → 온디바이스 AI가 제목/날짜/시간/장소 추출 → 확인 화면에서 수정 → 저장 → 캐릭터가 반응(먹이 획득 애니메이션)
2. **음성 등록**: 앱 실행 후 마이크 버튼 → "다음 주 화요일 3시 학원 상담" 발화 → 온디바이스 STT + 파싱 → 확인 화면 → 저장
3. **일정 이행 체크인**: 일정 시간이 되면 캐릭터가 알림 발화("나 데려가 줘! 3시 학원 상담이야") → 완료/미완료 체크 → 캐릭터 경험치 반영
4. **캐릭터 성장 확인**: 홈 화면에서 캐릭터 상태(레벨/기분/꾸미기 아이템) 확인, 성장 히스토리 열람

---

## 2. 기능 요구사항

### P0 (MVP 필수 — 이거 없으면 출시 불가)

**A. 캡처 기반 일정 추출**
- 스크린샷 촬영 감지 → 시스템 알림/오버레이로 "일정 등록" 제안
- 갤러리에서 이미지 직접 선택해 등록도 지원
- 온디바이스 OCR(ML Kit Text Recognition)로 텍스트 추출
- 온디바이스 LLM(Gemini Nano / ML Kit GenAI API)으로 제목·날짜·시간·장소 구조화 파싱
- 여러 일정이 한 이미지에 섞여 있을 경우 개별 항목으로 분리 (예: 시간표 캡처)
- 파싱 실패/모호한 항목은 "확인 필요" 상태로 사용자에게 수정 요청

**B. 음성 기반 일정 등록**
- 온디바이스 STT로 음성 → 텍스트 변환
- 동일한 파싱 엔진으로 일정 구조화
- 상대적 날짜 표현 처리 ("다음 주 화요일", "이번 주말")

**C. 등록 전 확인/수정 화면**
- AI가 추출한 제목/날짜/시간/장소를 사용자가 저장 전 반드시 검토·수정 가능
- (Dola/톡톡일정 등 경쟁사도 이 단계를 필수로 두는 이유: AI 오인식 방지 및 신뢰 확보)

**D. 캐릭터 시스템 (다마고치 코어 루프)**
- 캐릭터 알 부화 → 이름/외형 커스터마이징 (온보딩)
- 일정 등록 시 소량 경험치, 일정 "완료 체크"(제때 지킴) 시 더 큰 경험치
- 경험치 누적 → 레벨업 → 외형 변화/꾸미기 아이템 언락
- 일정 미이행 시 캐릭터가 "시무룩"해지는 정도의 약한 네거티브 피드백 (죄책감 유발 문구/디자인 금지)
- 캐릭터가 알림의 화자가 되어 다가오는 일정을 리마인드

**E. 캘린더 뷰 & 알림**
- 일/주/월 캘린더 뷰
- 일정별 알림 (기본 캘린더 알림 + 캐릭터 보이스 스타일 알림)
- 안드로이드 기본 캘린더 앱과 동기화(선택적, Calendar Provider API)

**F. 온보딩**
- 캡처/음성 권한 요청 및 "왜 필요한지" 온디바이스 프라이버시 설명
- 캐릭터 알 부화 첫 경험

### P1 (출시 직후 빠른 후속 업데이트)
- 카톡 대화 텍스트 붙여넣기로 일정 추출 (경쟁사 핵심 기능, 캡처보다 정확도 높음)
- 친구와 캐릭터 비교/자랑 공유 기능 (SNS 공유 카드)
- 반 시간표/학사일정 템플릿 등록
- 위젯 (홈 화면에서 캐릭터 상태 확인)

### P2 (중장기)
- 캐릭터 간 상호작용(친구 초대, 미니게임)
- 학습 습관 트래킹과 연동 (공부 시간 → 경험치)
- iOS 버전 (Foundation Models + Vision 프레임워크)

### MVP 제외 범위 (Not in scope)
- 실시간 서버 동기화/멀티 디바이스 (온디바이스 우선 원칙과 상충, 추후 옵션으로 검토)
- 유료 결제/캐릭터 아이템 상점 (P1 이후)
- 그룹/단톡방 일정 조율 (Dola의 강점 영역, 차별화 우선순위 낮음)

---

## 3. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 프라이버시 | 캡처 이미지/음성 데이터는 기기 밖으로 전송되지 않음. 처리 후 원본 이미지는 사용자 설정에 따라 즉시 삭제 옵션 제공 |
| 기기 호환성 | Gemini Nano(ML Kit GenAI API)는 일부 플래그십 기기만 지원 → 미지원 기기는 경량 규칙 기반 파싱(정규식 + 로컬 소형 모델) 폴백 로직 필요 |
| 성능 | 캡처 후 일정 제안까지 3초 이내 목표 |
| 배터리 | 백그라운드 OCR/LLM 상시 대기 대신 스크린샷 이벤트 트리거 방식으로 배터리 소모 최소화 |
| 오프라인 | 인터넷 연결 없이 전체 핵심 기능(캡처→등록→캐릭터) 동작 |

---

## 4. 화면 목록 (Screen Inventory)

1. 온보딩 (권한 요청 → 캐릭터 알 부화)
2. 홈 (캐릭터 + 오늘의 일정 요약)
3. 캡처 확인/수정 화면
4. 음성 입력 화면
5. 캘린더 (일/주/월)
6. 일정 상세/수정
7. 캐릭터 상세 (레벨, 성장 히스토리, 꾸미기)
8. 설정 (프라이버시, 알림, 권한 관리)

---

## 5. 성공 지표 (MVP 검증 KPI)

| 지표 | 목표(가설) |
|---|---|
| D1 리텐션 | 40%+ (캐릭터 온보딩 매력도 검증) |
| D30 리텐션 | Finch 벤치마크 대비 근접 목표 (자기관리 앱 최상위권 수준) |
| 캡처→등록 완료율 | 캡처 제안 알림 중 실제 저장까지 이어지는 비율 60%+ |
| AI 파싱 정확도 | 사용자가 수정 없이 그대로 저장하는 비율 70%+ |
| 일정 이행 체크인율 | 등록된 일정 중 완료 체크되는 비율 (캐릭터 루프 작동 검증) |

---

## 6. 리스크 및 검토 필요 사항

- **Gemini Nano 기기 커버리지**: 학생층은 중저가폰 비중이 높아, 온디바이스 LLM 미지원 기기 대응(폴백 파싱 품질)이 초기 사용자 경험을 좌우함
- **OCR 정확도**: 학원 앱/공지 캡처는 폰트/레이아웃이 다양해 다국어·비정형 텍스트 처리 테스트 필요
- **캐릭터 피로도**: 알림이 너무 잦으면 캐릭터에 대한 애정도 오히려 이탈 요인이 될 수 있음 → 알림 빈도 사용자 설정 필수
